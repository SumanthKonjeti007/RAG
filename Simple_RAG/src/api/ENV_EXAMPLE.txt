# Copy this content to src/api/.env and fill values

# Embeddings provider: mistral (default) or vertex
EMBEDDINGS_PROVIDER=mistral

# Mistral settings (required if EMBEDDINGS_PROVIDER=mistral)
MISTRAL_API_KEY=your_mistral_key_here
MISTRAL_EMBEDDINGS_MODEL=mistral-embed
MISTRAL_API_BASE=https://api.mistral.ai

# Vertex AI settings (required if EMBEDDINGS_PROVIDER=vertex)
# Uses Application Default Credentials (gcloud auth application-default login)
VERTEX_PROJECT_ID=your_gcp_project_id
VERTEX_LOCATION=us-central1
VERTEX_EMBEDDINGS_MODEL=textembedding-gecko@003

# OpenAI (used for intent, rewrite, generation, hallucination analysis)
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o-mini
